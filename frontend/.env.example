# Environment Variables Example
# Copy this file to .env.development or .env.production and adjust values as needed

# =============================================================================
# API Configuration
# =============================================================================

# Backend API base URL
# Development: Point to local ASP.NET Core backend
# Production: Use your deployed backend URL (e.g., Render, AWS, etc.)
VITE_API_BASE_URL=http://localhost:10000

# =============================================================================
# Development Settings
# =============================================================================

# Force development environment detection
# Set to 1 to ensure development mode even if NODE_ENV is set to production
VITE_FORCE_DEV=1

# =============================================================================
# Notes
# =============================================================================

# - All variables prefixed with VITE_ are exposed to the client-side code
# - For production, create .env.production and override VITE_API_BASE_URL
# - For local development, use .env.development (default: http://localhost:10000)
# - Backend should be running on the configured port (default: 10000)
